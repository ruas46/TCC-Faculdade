<nav role="navigation" class="navbar navbar-default">
  <div class="navbar-header">
    <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
      <span class="sr-only">Navegação</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
  </div>
  <div id="navbarCollapse" class="collapse navbar-collapse header-background-color header-text-prop">
    <ul class="nav navbar-nav">
      <li><a href="/<%= @acess %>/">Transtornos</a></li>
      <li><a href="/<%= @acess %>/sintoma">Sintomas</a></li>
      <li class="active"><a href="/<%= @acess %>/categoria">Categorias</a></li>
      <li><a href="/<%= @acess %>/transtornoNaoSolucionado">Transtornos sem resposta</a></li>
    </ul>
  </div>
</nav>
<table>
  <tr>
    <th>Nome</th>
    <th></th>
  </tr>
  <% @categorium.each do |category| %>
    <tr>
      <td><%= category.nome %></td>
      <td>
        <form action="/<%= @acess %>/editCategoria" method="post">
          <input type="hidden" name="id" value="<%= category.id %>">
          <button class="btn btn-primary" type="submit" value="Submit">Editar</button>
        </form>
      </td>
      <td>
        <form action="/<%= @acess %>/deleteCategoria" method="post">
          <input type="hidden" name="id" value="<%= category.id %>">
          <button class="btn btn-primary" type="submit" value="Submit">Deletar</button>
        </form>
      </td>
    </tr>
  <% end %>
</table>
<a href="/<%= @acess %>/novaCategoria">Nova Categoria</a>
